ext {
    androidBuild = [
            versionName: "1.8.0",
            versionCode: 180,
    ]
    GROUP = 'com.android.update'
    POM_URL = 'http://localhost:8081/repository/druid/'
    POM_PACKAGING = 'aar'
}
